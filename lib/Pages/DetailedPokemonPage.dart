import 'package:flutter/cupertino.dart';

class DetailedPokemonPage extends StatelessWidget {
  const DetailedPokemonPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
